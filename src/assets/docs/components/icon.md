```css
fiv-icon {
  --fiv-padding-end: 0;
  --fiv-padding-start: 0;
  --fiv-color-icon: var(--ion-color-dark);
  --fiv-dot-color-background: var(--ion-color-light);
  --fiv-color-indicator: var(--ion-color-primary);
}
```

```html
<fiv-icon [indicatorValue]="indicatorValue" [name]="icon"></fiv-icon>
```
